<%if @user.products.present?%>
  <% @user.products.each do |product|%>
	<%=link_to "#{product.title.capitalize}", product_path(product.id)%>
  <%end%>
<%else%>
  "User doesn't add any product"
<%end%> 