= form_for @line_item do |f|
  - if @line_item.errors.any?
  = errors_for(@line_item)

  .field
    = f.label :product
    = f.text_field :product
  .field
    = f.label :cart
    = f.text_field :cart
  .actions
    = f.submit 'Save'
